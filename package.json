{
  "name": "rest-to-json-schema",
  "version": "1.0.0",
  "description": "get the json schema from a rest endpoint",
  "main": "index.js",
  "author": "LIT frontend",
  "license": "MIT",
  "scripts": {
    "generate:schema": "ts-node index.js"
  },
  "devDependencies": {
    "json-schema-generator": "^2.0.6",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
